name: Build & Deploy Frontend

on:
  push:
    branches: [master]

env:
  ACR:      crmtreeregistry.azurecr.io
  ACR_USER: crmtreeregistry
  APP_NAME: crmtree-frontend
  RG:       rg-crmtree-prod

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to ACR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.ACR }}
          username: ${{ env.ACR_USER }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build Docker image
        run: docker build -t ${{ env.ACR }}/${{ env.APP_NAME }}:${{ github.run_number }} .

      - name: Push to ACR
        run: docker push ${{ env.ACR }}/${{ env.APP_NAME }}:${{ github.run_number }}

      - name: Deploy to Azure Container App
        run: |
          az login --service-principal \
            -u ${{ secrets.AZURE_CLIENT_ID }} \
            -p ${{ secrets.AZURE_CLIENT_SECRET }} \
            --tenant ${{ secrets.AZURE_TENANT_ID }}
          az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          az containerapp update \
            --name ${{ env.APP_NAME }} \
            --resource-group ${{ env.RG }} \
            --image ${{ env.ACR }}/${{ env.APP_NAME }}:${{ github.run_number }}
