server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri $uri/ /index.html; }
  location /api {
    proxy_pass https://api.crmtree.pl;
    proxy_set_header Host api.crmtree.pl;
    proxy_ssl_server_name on;
  }
}